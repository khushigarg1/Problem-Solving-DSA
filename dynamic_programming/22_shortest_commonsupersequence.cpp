// shortest common supersequence length
/*
    s1 = brute  s2 =good
    then make such a string such that which includes both the string
    like brutegood
    but we need smallest length bgruoote length = 8 overall 10
*/
#include <bits/stdc++.h>
using namespace std;

int findcommon(int s1len, int s2len, string s1, string s2)
{
    if ()
}
int main()
{
    string s1, s2;
    cin >> s1 >> s2;
    cout << findcommon(s1.size() - 1, s2.size() - 2, s1, s2);
    return 0;
}