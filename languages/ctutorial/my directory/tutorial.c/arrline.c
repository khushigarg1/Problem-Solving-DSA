#include<stdio.h>

#include<conio.h>
int main()
/*{
    
    int flag=0,value=5;
    int a[5]={1,2,3,4,5};
    for(int i=0;i<5;i++)
    {
        if (a[i]==value)
        {
            flag=1;
            break;
        }
    }
    if (flag==1)
    printf("element is present");
    else
    printf("element is not present");
}*/

{
    int i,value=6,c;
    int a[5]={1,2,3,4,5};

    for(i=0;i<5;i++)
    {
        if(a[i]==value)
        {
             c==1;
        }
    }

     if(c==1)
     printf("element is present");
     else
     printf("elements is not present");
      return 0;
}